============================
PLANBOOKAI - QUICK COMMANDS
============================

DATABASE:
  Start:  .\scripts\start-dev-db.ps1
  Stop:   .\scripts\stop-dev-db.ps1

DOCKER:
  Logs:   docker logs planbookai-postgres-dev
  Shell:  docker exec -it planbookai-postgres-dev psql -U test -d planbookai
  Reset:  docker-compose -f docker-compose.dev.yml down -v

PSQL COMMANDS:
  \dt users.*        - Xem tables trong users schema
  \dt content.*      - Xem tables trong content schema  
  \dt assessment.*   - Xem tables trong assessment schema
  \dt students.*     - Xem tables trong students schema
  \q                 - Thoat psql

CONNECTION:
  Host: localhost:5432
  DB: planbookai
  User: test / test123

TROUBLESHOOT:
  1. Docker Desktop phai chay
  2. Port 5432 phai trong
  3. Dung PowerShell (khong CMD)

============================